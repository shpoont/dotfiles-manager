version: 2

project_name: dotfiles-manager
builds:
  - id: linux
    main: ./cmd/dotfiles-manager
    binary: dotfiles-manager
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=0
    ldflags:
      - -X github.com/shpoont/dotfiles-manager/internal/app.buildVersion={{ .Version }}
  - id: darwin
    main: ./cmd/dotfiles-manager
    binary: dotfiles-manager
    goos:
      - darwin
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=0
    ldflags:
      - -X github.com/shpoont/dotfiles-manager/internal/app.buildVersion={{ .Version }}
archives:
  - id: default
    ids:
      - linux
      - darwin
    formats:
      - tar.gz
    files:
      - README.md
      - docs/**
checksum:
  name_template: '{{ .ProjectName }}_{{ .Version }}_checksums.txt'

release:
  # Mark semantic-version prerelease tags such as v0.2.0-rc.1 as GitHub
  # pre-releases. The v2 release-candidate publication gate must not rely on
  # post-publish manual correction for prerelease status.
  prerelease: auto
