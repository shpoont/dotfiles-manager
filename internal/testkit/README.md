# internal/testkit

Planned shared test helpers for:

- `sandbox/`: temporary filesystem/home sandbox setup
- `fixtures/`: fixture materialization helpers
- `cli/`: command runner wrappers
- `asserts/`: common assertions for JSON/log/filesystem

This scaffold exists so future tests can converge on one helper structure.
