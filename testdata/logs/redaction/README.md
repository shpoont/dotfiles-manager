# Redaction fixtures

Fixtures related to logging redaction and masking behavior.

Current coverage validates runtime redaction through command tests that use sensitive-looking
values in config paths and error messages.
